<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.cardview.widget.CardView
        android:id="@+id/cvMemberProfileImg"
        android:layout_width="70dp"
        android:layout_height="70dp"
        app:cardCornerRadius="20dp"
        android:padding="4dp"
        android:layout_margin="14dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">
        <ImageView
            android:id="@+id/ivMember"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/bolkar"/>
    </androidx.cardview.widget.CardView>
    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/cvMemberProfileImg"
        app:layout_constraintStart_toStartOf="@id/cvMemberProfileImg"
        app:layout_constraintEnd_toEndOf="@id/cvMemberProfileImg"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        tools:text="name"/>
</androidx.constraintlayout.widget.ConstraintLayout>